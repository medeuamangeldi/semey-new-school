import styles from "./portal.module.scss";

const Portal = () => {
  return (
    <div className={styles["portal-container"]}>
      <h1 className={styles["portal-container-title"]}>Portal</h1>
    </div>
  );
};

export default Portal;
