import styles from "./profile.module.scss";

const Profile = () => {
  return (
    <div className={styles["profile-container"]}>
      <h1 className={styles["profile-container-title"]}>Profile</h1>
    </div>
  );
};

export default Profile;
